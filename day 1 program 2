#include <stdio.h>
int main()
{
	int num;
	printf("enter an integer :");
	scanf("%d",&num);
	if(num %5==0 && num % 11==0)
	printf("number dibisible by 5 and 11\n");
	else 
	printf("number is not divisible by 5 and 11\n");
	return 0;
	}
